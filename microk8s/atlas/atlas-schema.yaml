apiVersion: db.atlasgo.io/v1alpha1
kind: AtlasSchema
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  name: jdw-non-atlas-schema
spec:
  credentials:
    scheme: auth
    host: postgresql-cluster-non-rw
    user: postgres
    passwordFrom:
      secretKeyRef:
        key: password
        name: pg-password
    database: jdw
    port: 5432
    parameters:
      sslmode: disable
  schema:
    configMapKeyRef:
      key: schema.sql
      name: jdw-postgresql-non-schema