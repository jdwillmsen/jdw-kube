apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: gha-runner-scale-set-controller-secrets
  namespace: arc-runners
rules:
  - apiGroups: [""]
    resources: ["*"]  # All core resources (secrets, configmaps, pods, etc.)
    verbs: ["*"]      # All actions (get, list, create, delete, etc.)
  - apiGroups: ["rbac.authorization.k8s.io"]
    resources: ["*"]  # All RBAC resources (roles, rolebindings)
    verbs: ["*"]
  - apiGroups: ["actions.github.com"]
    resources: ["*"]  # All ARC custom resources
    verbs: ["*"]
